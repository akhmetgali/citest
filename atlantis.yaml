version: 3
automerge: true
projects:
  - name: citest
    dir: citest
    workspace: default
    terraform_version: v0.11.0
    workflow: myworkflow
workflows:
  myworkflow:
    plan:
      steps:
        - run: echo "Test Plan"
        - init
        - plan
    apply:
      steps:
        - run: echo "test apply"
        - apply
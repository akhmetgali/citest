version: 3
automerge: true
projects:
  - name: create-ec2
    dir: create-ec2
    workspace: default
    terraform_version: v0.11.0
    apply_requirements: [mergable]
    workflow: myworkflow
workflows:
  myworkflow:
    plan:
      steps:
        - run: echo "Test Plan"
        - init
        - plan
    apply:
      steps:
        - run: echo "test apply"
        - apply
version: 3
automerge: true
projects:
  - name: citest
    dir: citest
    workspace: default
    terraform_version: v0.11.0
    autoplan:
      when_modified: ["*.tf", "../modules/**.tf"]
      enabled: true
    workflow: myworkflow
workflows:
  myworkflow:
    plan:
      steps:
        - run: echo "Test Plan"
        - init
        - plan
    apply:
      steps:
        - run: echo "test apply"
        - apply